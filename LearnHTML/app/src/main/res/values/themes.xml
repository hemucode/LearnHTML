<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LearnHTML" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor" tools:targetApi="21">@color/deep_green</item>
    </style>

    <style name="Theme.LearnHTML.WebView" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/green</item>
        <item name="android:statusBarColor" tools:targetApi="21">@color/deep_green</item>
    </style>

    <style name="Theme.LearnHTML" parent="Base.Theme.LearnHTML" />
</resources>